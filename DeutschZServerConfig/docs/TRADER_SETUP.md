# Trader Setup

Expansion market format was matched from the local server profile. Currency is `expansionbanknotehryvnia`.

Add the generated market JSON files to `profiles/ExpansionMod/Market/`, then add categories from `trader/*.example.json` to the appropriate live trader files.
